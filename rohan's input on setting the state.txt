comp 1
const [tableState,setTableState] = useState([])

const [state,setState] = useState({
name:"",
content:""
})

<comp2 tableState={tableState} setTableState={setTableState}/>

const editClicck = (i)=>{
	tableState.map((item,index)=>{
		if(index===i){setState(item)}
	})
setModalOPen(true)
}

comp2




const handleChange = (e)=>{
	const {name,value} = e.target
	setState(prev=>({...prev,[name]:value}))
}

const handleSubmit = ()=>{

const temp = [...tableState]
temp.push(state)
setTableState(temp)
}